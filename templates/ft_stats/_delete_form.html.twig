<form method="post" action="{{ path('app_ft_stats_delete', {'id': ft_stat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ft_stat.id) }}">
    <button class="btn">Delete</button>
</form>
